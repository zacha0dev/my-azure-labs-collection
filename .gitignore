# =========================
# Universal Python Project
# =========================

# --- Python bytecode / caches ---
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
*.so

# --- Build artifacts ---
.Python
build/
dist/
downloads/
develop-eggs/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
share/python-wheels/
pip-wheel-metadata/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- Virtual environments (DO NOT COMMIT PYTHON ENV DATA) ---
# Adjust if you keep env in a custom path
.venv/
.venv*/
venv/
venv*/
env/
ENV/
env*/
.python_packages/
__pypackages__/

# --- Dependency managers (keep lockfiles if you use them) ---
# Pipenv/Poetry lockfiles are usually committed, so NOT ignored by default
# Pip caches/logs
pip-log.txt
pip-delete-this-directory.txt

# --- Testing / Coverage / Type checking ---
.pytest_cache/
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
htmlcov/
*.cover
*.py,cover
.hypothesis/
.mypy_cache/
.pyre/
.pytype/
.dmypy.json
dmypy.json
.cache/

# --- Notebooks ---
.ipynb_checkpoints/

# --- Logs ---
logs/
*.log
*.log.*

# --- Dotenv (secrets); keep an example committed ---
.env
.env.*
!.env.example

# --- OS junk ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Icon?
ehthumbs.db
Thumbs.db
desktop.ini

# --- Editors / IDEs ---
.vscode/
*.code-workspace
.idea/
*.iml
.vscode-test/
.history/
*.sw?
*~
*.orig
*.bak
*.tmp

# --- Docker (optional) ---
# Keep compose files you intend to commit; ignore local overrides
docker-compose.override.yml

# --- Node (optional, if you add UI later) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp.*
.pnpm-debug.log*

# --- Terraform (optional) ---
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# --- Certificates / secrets / DBs ---
*.pem
*.key
*.crt
*.p12
*.der
*.csr
*.jks
secrets*.json
*.sqlite3
*.db

# =========================
# Project-specific ignores
# =========================

# Router heartbeat (generated by router process)
router/.heartbeat

# Runtime config (commit template only)
config/app.json
# keep template committed:
# config/template.app.json  <-- DO NOT ignore

# Local run artifacts you may create
*.pid
*.lock

# If you vendor libraries (not recommended), ignore here:
lib/
lib64/
site-packages/

# If you produce temporary scratch data:
tmp/
temp/